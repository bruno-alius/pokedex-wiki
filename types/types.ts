type Pokemon = {
    name: string
    alias: string
    moves: string[]
    moveImgs: string[]
    editMode: boolean
    tags: string
    typeImgs: string[]
}

type Item = {
    name: string
    editMode: boolean
}